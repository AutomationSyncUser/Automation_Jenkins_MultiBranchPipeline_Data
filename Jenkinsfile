node {

   properties([
     parameters([
       string(name: 'OH_ID', defaultValue: '0')
     ])
   ])
   
   // Mark the code checkout 'stage'....
   stage 'Checkout'

   // Checkout code from repository
   checkout scm
}
